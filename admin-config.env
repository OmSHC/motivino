# Persistent Admin Configuration
# This file contains admin user credentials that will be stored in the database
# Edit these values before running deployment or admin-setup.sh

# Admin User Credentials (stored permanently in database)
ADMIN_EMAIL=admin@motivino.com
ADMIN_PASSWORD=change-this-password-immediately!
ADMIN_FIRST_NAME=System
ADMIN_LAST_NAME=Administrator

# Instructions:
# 1. Edit the values above with your desired admin credentials
# 2. Run: source admin-config.env && ./gcp-production-deploy.sh
# 3. Or run: source admin-config.env && ./admin-setup.sh
#
# The admin user will be created in the database and persist across deployments
